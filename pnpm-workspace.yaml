# Standalone single-package plugin repo: an empty workspace boundary stops pnpm
# from walking up into the harness monorepo (whose workspace does not include
# this package). The root package is the only project; devDependencies (tsdown,
# the bundled @codemirror/* runtimes) install with it.
packages: []
